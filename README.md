# Data Import Service

Authenticates against an external API, fetches employee records, validates and
stores them in SQLite, and exposes them through a FastAPI HTTP service.

See [CLAUDE.md](CLAUDE.md) for the full specification, commands, and architecture.
